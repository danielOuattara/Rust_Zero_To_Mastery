/// A favorite color
enum Color {
    Red,
    Blue,
}

/// A piece of email
struct Mail {
    address: String,
}

/// Adds 2 numbers together
fn add(a: i32, b: i32) -> i32 {
    a + b
}

fn main() {}

// the run in terminal: "cargo doc --open" to run and see the docs
