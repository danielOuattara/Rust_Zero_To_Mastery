// check if a vector is empty

fn main() {
    let numbers = vec![1, 2, 3];

    match numbers.is_empty() {
        true => println!("No items found in vector"),
        false => println!("Has items in vector"),
    }
}

// to access the Rust doc: rustup doc

// activity @17
